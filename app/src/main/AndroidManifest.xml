<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.exaco.internship.android.weatherdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WeatherApplication_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.weather.WeatherActivity_"
            android:screenOrientation="nosensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.city.CityActivity_"
            android:screenOrientation="nosensor"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.weather.WeatherActivity" />

        <activity
            android:name=".ui.weather.FutureWeatherActivity_"
            android:label="@string/title_activity_future_weather"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ui.weather.FutureWeatherActivity"></activity>
    </application>

</manifest>